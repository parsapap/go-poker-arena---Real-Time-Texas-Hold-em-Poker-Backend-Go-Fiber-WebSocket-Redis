# Build context is ./backend, so paths are relative to it.
.env
*.env
!.env.example
*.log
tmp/
bin/
coverage.out
coverage.html
README.md
Makefile

# Load/stress/integration helpers are not needed in the runtime image.
test/
